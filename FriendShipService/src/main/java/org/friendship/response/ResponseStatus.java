package org.friendship.response;

public enum ResponseStatus {
    SUCCESS,
    FAILURE,
    USER_NOT_FOUND,
    INVALID_PASSWORD,
    USER_ALREADY_EXISTS,
    TOKEN_INVALID,
    UNKNOWN_ERROR,
    ERROR, VALIDATE
}
